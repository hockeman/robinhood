# Run 2026-09-25 17:20 UTC (12:20 CT / 13:20 ET Friday) — BORR stop-out reconciled, no new entry

## Orientation
- Followed `PERSISTENCE.md`: `git fetch origin claude/trading-state` then `git checkout -B claude/trading-state origin/claude/trading-state`. Clean fetch, continuing from the prior run's push (2026-09-25T16:25:00Z).
- Read `config.json`, `state.json`, `notification_state.json`, `AGENT.md`, `PERSISTENCE.md`, `RECONCILIATION.md`, and the latest journal entry. `STOP` not present.
- config.json/AGENT.md's aggressive parameters continue to be honored as genuine, previously-verified owner commits (established in prior runs' `git log --follow -p`, not re-run from scratch this hour); numeric/rotation overrides honored, "regular hours only" and "day-of-earnings forbidden" remain unconditional hard rules regardless of config keys.
- Time: 13:15–13:20 ET (Friday 2026-09-25) — regular session open (9:30–16:00 ET).

## Step 1 — Account snapshot
- `get_accounts`: account `604824292` ("Agentic") is the only `agentic_allowed=true` account. Used exclusively.
- `get_portfolio`: total_value $7,372.3083, cash $122.23, unleveraged buying power $122.23.
- `get_equity_positions`: MSTR 23, AAPL 0.005949 (legacy, unmanaged), SBLK 57, ANGX 4.329986, DUOT 207. **BORR (26 sh) is gone** — not in positions, not in open orders.
- Reconciliation: `get_equity_orders(symbol=BORR)` shows the standing stop-market order (id `6ab13b9c-...`, stop $4.11) is now `filled` — execution `6ab6a4bc-...`, 26 sh @ avg $4.1100, timestamp `2026-09-25T16:43:40.031Z`. Entry was 2026-09-21 @ $4.4685; exit 2026-09-25 — 4 calendar days held, **not a day trade**. Realized P&L: -$9.32 (-8.0% of $116.18 cost basis).
- `state.json` updated: removed BORR from `positions`, appended to `closed` (exit_reason `stop_loss`), added `stopped_out_recently.BORR` with cooldown through 2026-09-26 (config `reentry_cooldown_days_after_stop`=1). Appended matching `exit` record to `trades.jsonl`.
- `account_value_history` appended: `{"ts":"2026-09-25T17:20:00Z","value":7372.308256685}`. `peak_account_value` unchanged at $7,954.86462317 — current value is 7.32% below peak, well under the 40% drawdown halt.
- Day trades (rolling 5 business days): 0/2 used (BORR exit spanned 4 days, not same-day).

## Step 2 — Protect and manage exits
- `get_equity_quotes` on all held symbols: MSTR $158.89 (high_since_entry $171.17, unchanged — not a new high), SBLK $29.59 (high $30.60, unchanged), ANGX $4.81 (high $4.8499, unchanged), DUOT $9.125 (high $9.58, unchanged) — none at a new high, so no trail-stop replacement was due on any position.
- All three protectable open positions (MSTR, SBLK, DUOT) re-verified with a live, confirmed protective stop, unchanged since last run.
- ANGX and AAPL remain structurally unprotectable (fractional lots) — standing, owner-accepted risk, unchanged.

## Step 3 — Entry gates
- STOP: not present. Drawdown: 7.32% below peak (halt at 40%, not tripped). Day trades: 0/2.
- Open positions: 4 tracked (MSTR, SBLK, ANGX, DUOT) + 1 unmanaged legacy AAPL lot — at/above `config.json`'s `max_open_positions` (3).
- Buying-power gate: cash freed by the BORR stop-out brought unleveraged buying power to $122.23; $122.23 − $10 reserve = $112.23, **above** `min_position_usd` ($100) for the first time in several runs. A fresh-cash entry is arithmetically possible if a pyramid or rotation slot exists.
- Checked for a pyramid slot on a held winner (only mechanism that doesn't require rotation): MSTR is up 5.8% from entry (≥ `pyramid_min_unrealized_pct` 5%), `pyramids_done`=0 < `max_pyramids_per_position`=2 — but MSTR's current position value (23 × $158.89 = $3,654.47) is already ~49.6% of account equity, which **exceeds its score-tier size cap** (MSTR is `signal: legacy`/score 0 → tier cap 40% under AGENT.md sizing). The pyramid rule requires combined value after the add to stay ≤ the tier cap, and it is already over cap before any add — **pyramid rejected**. SBLK, ANGX, DUOT are all flat-to-down from entry, not eligible. No pyramid slot exists.
- With no pyramid slot and open positions ≥ max, the only remaining path to deploy the freed cash into a *new* name is a rotation (sell a weaker holding, net position count unchanged) clearing the rotation bar (best-held score DUOT=10 + `rotation_min_score_advantage` 2 = **≥12** required). Proceeded to a light Step 4 refresh to check for a rotation-worthy candidate.

## Step 4 — Gather signals (abbreviated refresh, ~55 min since last full sweep)
- OpenInsider (`latest-cluster-buys`): **HTTP 503**, retried once, still 503 — source outage this run, journaled rather than fabricated.
- CapitolTrades: not re-checked this run (already known down for many consecutive runs per prior journals); no new attempt made given OpenInsider was also down and neither source would change the rotation-bar conclusion without corroboration from the other anyway.
- Re-checked last run's leading candidate, **CYBN** (score 10): `get_equity_quotes(["CYBN"])` returned `inactive_instruments` — CYBN is no longer an active/tradable instrument. **Disqualified**, not just chase-blocked. Removing it from the active candidate list going forward.
- No market-wide, symbol-agnostic discovery source was reachable this run (EDGAR's per-symbol filing index requires already knowing a ticker; it cannot substitute for OpenInsider/CapitolTrades discovery). No fresh candidate could be sourced or verified this run.
- Given no reachable discovery source and no verified candidate, no rotation bar could even be evaluated this run — not "failed to clear the bar" but "no data to evaluate." No trade placed on unverified information (MNPI/verification guard).

## Step 5 — Entry
Not reached — no verified candidate, no pyramid slot, and open positions already ≥ cap.

## Top candidates this run
None verifiable — both primary discovery sources unreachable (OpenInsider 503, CapitolTrades known-down). Last run's top candidate (CYBN, score 10) is now delisted/inactive and removed from consideration.

## Passed on eligible name?
No — there was no verifiable eligible name to pass on this run; the block was source availability, not a scoring judgment call. The freed BORR cash ($112.23 spendable) sits idle only because no candidate could be verified, not because of unwillingness to deploy it.

## Experiment fields (required every run, including silent runs)
- Top 5 candidates with scores: none available this run (OpenInsider 503 x2, CapitolTrades down; CYBN from last run now inactive/delisted).
- Passed on an eligible name, and why: no — no eligible name was verifiable this run (source outages), not a discretionary pass.
- Current drawdown from peak: 7.32% ($7,372.31 vs. $7,954.86 peak).
- Remaining PDT slots (rolling 5 business days): 2/2 unused.
- MNPI_GUARD events: none.

## Step 7 — Weekly statistics
Not due this run — today (Friday, the week's last session) has not closed yet (regular session in progress, closes 16:00 ET / 20:00 UTC). Will fold into today's DAILY_CLOSE once the session closes.

## Step 8 — Notification gate
- `notification_state.json`: 2026-09-24 DAILY_CLOSE already `verified_sent`; today's session hasn't closed, so no DAILY_CLOSE due yet.
- New qualifying event this run: BORR position closed (automatic stop fill) — a material change per `reporting/README.md`'s ACTION_UPDATE trigger list ("a position closed"). Sent **one** ACTION_UPDATE (report ID `action-acct4292-2026-09-25T1720Z`), recipient `ferrell@chacetech.com`. Verified `SENT` via `get_message(1a0d99529dc23b81)` — `labelIds` includes `SENT`, correct recipient, dated `2026-09-25T17:20:26Z`. Outbox record upgraded from `prepared` to `verified_sent`; `reported_event_ids` updated with `exit:BORR:2026-09-25T16:43:40Z`. Rendered copy saved to `reporting/sent/action_update_2026-09-25T1720Z.html`.
- Only one email sent this run, within `max_action_emails_per_run` (1).

## Notes for next run
- CYBN is now confirmed inactive/delisted — remove permanently from the candidate watchlist, do not re-surface.
- OpenInsider returned HTTP 503 this run (retried once) — re-check next run; if it persists across multiple runs, treat as a standing source outage worth flagging to the owner, similar to the long-running CapitolTrades 429s.
- Cash is now $112.23 above reserve (post-BORR-stop) — first time in several runs a fresh-cash entry is arithmetically possible; still gated on open-position count (4 ≥ cap 3) requiring either a pyramid (none currently eligible — MSTR already over its tier size cap) or a rotation clearing the ≥12 bar. Prioritize a full Step 4 sweep next run once discovery sources are reachable again.
- Carried-forward, unchanged items: AAPL/ANGX fractional lots (unprotected, owner-accepted), advanced/OCO orders SERVICE_DISABLED (standing constraint), GitHub default-branch setting still not pointed at `claude/trading-state` (one owner action outstanding).
- BORR reentry cooldown active through 2026-09-26 — do not re-enter BORR before then even if a fresh insider print appears.

## Save
Committing `state.json` (BORR moved to `closed`, `stopped_out_recently` updated, `last_run`, appended `account_value_history` entry), `trades.jsonl` (BORR exit line), `notification_state.json` (new outbox record, `reported_event_ids` update), `reporting/sent/action_update_2026-09-25T1720Z.html`, and this journal entry on top of the `origin/claude/trading-state` tip fetched at the start of this run, then `git push origin HEAD:claude/trading-state` (plain, non-force) per `PERSISTENCE.md`.

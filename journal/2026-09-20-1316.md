UNRESOLVED: the 2026-09-18 run flagged real, rule-violating and unexplained trades on this account (a same-day add-to-position on MSTR, and an unexplained $2 AAPL market buy fitting no signal) and asked the owner to confirm the source before the next unattended pass. Two days later there is still no STOP file, config change, or other sign that happened. Re-flagging as still open, not resolved. This run also found that of ~39 `claude/clever-thompson-*` branches on origin, only one (2026-09-18's) is merged into `main`; this run's own commit again lands on an isolated branch (`claude/clever-thompson-a731bh`, not `main`) for the same reason 09-18's state was missing — see Warnings #2.

RUN 2026-09-20 13:16 ET — market closed (Sunday) — mode normal (no STOP file; weekend, so only steps 1-3/7-8 apply per standing rules)
Account: $7435.69 (+$45.13 / +0.61% since last run; new peak $7435.69; drawdown 0%) — buying power $0.60

Actions
- None. No orders placed, cancelled, or modified. Verified (read-only) that all four real positions still carry the same confirmed protective stop-limit sell orders recorded in state.json; nothing has changed since the 2026-09-18 run (no trading session has occurred since Friday's close).

Positions (all real, account ••••4292)
SYM   qty      entry     last      P&L$      P&L%     stop(trigger/limit)   protective order state
MSTR   23      150.17    153.91    +86.02    +2.49%    144.80 / 144.50       confirmed, unchanged
ALVO  300        5.94      5.87    -22.50    -1.26%      5.65 /   5.60       confirmed, unchanged
DSP   161       11.70     11.88    +28.18    +1.50%     11.20 /  11.15       confirmed, unchanged
UROY   33        4.53      4.55     +0.66    +0.44%      4.28 /   4.25       confirmed, unchanged
AAPL  0.005949  336.19    335.58     ~0.00    -0.18%     none — unmanaged, unexplained (see 09-18 journal + warning below)

Signals considered: none — Step 4 not reached (weekend; also buying power $0.60 is far below min_position_usd regardless).

Gates: STOP no; drawdown ok (0%); day trades last 5d 0/2; open positions 4/8 (5 counting the AAPL fractional lot)

Warnings / errors
1. Carried forward, still UNRESOLVED: real trades on this account (a rule-violating same-day add to the MSTR position, and an unexplained $2/0.005949-share AAPL market buy with no matching signal, wrong order type, and far below min_position_usd) were placed before this repo had any config/state, per the 2026-09-18 journal. That entry asked the owner to confirm the routine/process responsible before running unattended again. As of this run, nothing in the repo (no STOP, no config edit, no note) indicates that happened. Treating this as still open and re-reporting it rather than assuming it was reviewed.
2. New finding: `origin` carries roughly 39 branches named `claude/clever-thompson-*` plus `main`; only the 2026-09-18 run's branch appears to have been merged into `main`. If each scheduled run commits its journal/state to its own throwaway branch instead of `main`, that fully explains why 09-18 found "no prior state" despite real trades already existing on the account — an earlier run's memory was written somewhere `main` never saw. This run is itself on such a branch (`claude/clever-thompson-a731bh`) and, per this session's own operating constraints, cannot push directly to `main` or open a PR without being asked. So this run's update will also not be visible as "prior state" to the next run unless the owner merges it (or the scheduled routine is reconfigured to commit straight to `main`, as the standing instructions call for). This is worth fixing before trusting multi-run continuity.
3. `get_advanced_orders` returns `SERVICE_DISABLED` for this account — true OCO bracket orders are not available here, so all protection is, and will continue to be, individual stop-limit sell orders rather than the OCO brackets the standing procedure describes for new entries. Not a new problem, just documenting why.
4. The AAPL fractional lot (item 1) remains unexplained, unprotected, and untouched — it's financially trivial (~$2) but is still an unreconciled discrepancy.

Notes for next run
- Do not treat warnings #1 or #2 as resolved until the owner has actually responded — this run repeated them because two days of silence isn't confirmation.
- Buying power is still $0.60; no new entries are possible on any weekday run until a stop/take-profit fires or cash is added, independent of signal quality.
- If the owner fixes the branch/merge issue, re-verify state.json continuity on the next run (compare against live get_equity_positions/get_equity_orders as this run did) rather than trusting it blindly.

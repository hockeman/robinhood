# Run 2026-09-25 16:25 UTC (11:25 CT / 12:25 ET Friday) — mid-session reconciliation, no trading action

## Orientation
- Followed `PERSISTENCE.md`: `git fetch origin claude/trading-state` then `git checkout -B claude/trading-state origin/claude/trading-state`. Clean fetch, continuing from the prior run's push (2026-09-25T15:24:00Z).
- Read `config.json`, `state.json`, `notification_state.json`, `AGENT.md`, `PERSISTENCE.md`, `RECONCILIATION.md`, and the latest journal entry. `STOP` not present.
- Re-verified config.json/AGENT.md's aggressive parameters are genuine owner (hockeman) commits via prior runs' `git log --follow -p` findings (already established, not re-run from scratch this hour) — honored as authoritative per the standing interpretation (numeric/rotation overrides honored; "regular hours only" and "day-of-earnings forbidden" remain unconditional hard rules regardless of config keys).
- Time: 12:15–12:25 ET (Friday 2026-09-25) — regular session open (9:30–16:00 ET).

## Step 1 — Account snapshot
- `get_accounts`: account `604824292` ("Agentic") is the only `agentic_allowed=true` account. Used exclusively.
- `get_portfolio`: total_value $7,394.6954, cash $15.37, unleveraged buying power $15.37.
- `get_equity_positions`: MSTR 23, AAPL 0.005949 (legacy, unmanaged), BORR 26, SBLK 57, ANGX 4.329986, DUOT 207 — exactly matches `state.json`. No new position, none missing, no stop-out since the last run.
- `get_equity_orders` (state=confirmed): all 4 protective stops re-verified `confirmed`, unchanged — MSTR stop $152.55/limit $152.25, BORR stop_market $4.11, SBLK stop_market $25.49, DUOT stop_market $7.54.
- `account_value_history` appended: `{"ts": "2026-09-25T16:25:00Z", "value": 7394.6953918136}`. `peak_account_value` unchanged at $7,954.86462317 — current value is 7.04% below peak, well under the 40% (config.json) drawdown halt.
- Day trades (rolling 5 business days): 0/2 used. `stopped_out_recently`: only UROY (cooldown until 2026-10-21, not held).
- Down from last run's snapshot (~1hr earlier): total value down from $7,450.88 to $7,394.70 (-0.75%), tracking a broadly red morning across the book (MSTR -1.52%, BORR -2.46%, SBLK -1.30%, DUOT -3.46%, ANGX -0.41% vs yesterday's close).

## Step 2 — Protect and manage exits
- `get_equity_quotes` on all held symbols: MSTR $159.15 (high_since_entry $171.17), BORR $4.155 (high $4.575), SBLK $29.51 (high $30.60), DUOT $9.22 (high $9.58), ANGX $4.8401 (high $4.8499) — none at a new high today, so no trail-stop replacement was due on any position.
- Every protectable open position (MSTR, BORR, SBLK, DUOT) has a live, confirmed protective order, unchanged since last run — re-verified directly against the broker.
- ANGX and AAPL remain structurally unprotectable (fractional lots, broker rejects stop orders on fractional quantities) — standing, owner-accepted risk, unchanged, not re-litigated.

## Step 3 — Entry gates
- STOP: not present. Drawdown: 7.04% below peak (halt at 40%, not tripped). Day trades: 0/2. Open positions: 5 tracked (+1 unmanaged legacy AAPL lot), above `config.json`'s `max_open_positions` (3) — carried forward, addressable only via rotation.
- Buying-power gate fails for a fresh-cash entry: $15.37 cash − $10 reserve = $5.37 available, far below `min_position_usd` ($100). A new position can only happen via rotation.
- Rotation bar: best held score is DUOT at 10; `rotation_min_score_advantage` (2) means a candidate needs score ≥ 12. No legacy/scoreless holding is currently down enough today to flip the alternate rotation path meaningfully.

## Step 4 — Gather signals
Delegated live research (OpenInsider all three pages, SEC EDGAR full-text-search + raw Form 4 XML cross-verification, CapitolTrades) to a sub-task.
- CapitolTrades: unreachable again (HTTP 429) — politician data and insider+politician confluence unverifiable this run, same as every recent run.
- Strongest new candidate: **CYBN** — score 10 (Exec Chairman/CGO Glavine $1.18M buy 9/3 + Exec Chair So $1.24M buy 9/8, 5-day gap; CEO Halstead $1.35M buy, ownership +146%; all EDGAR-verified code-P Form 4s). Ties DUOT (weakest-scored held position, also 10) but does **not** clear the 12-point rotation bar.
- Also verified and scored below the bar: UBER (8), PNFP (9, correctly excluded a CFO gift transaction), BCBP (8), DY (7, cluster span 22 days just misses the 21-day window), GRNT (~6).
- Two names explicitly flagged as non-signals: GLOO (Thrivent 10%-owner tranche filed 66 days late — compliance red flag, not a fresh print) and ETRA (two $20M "purchases" at an identical $15.00 price by a director and a fund — looks like a PIPE/private placement, not open-market informed flow; not scored).
- No 13D filings surfaced. No candidate reached the ≥12 rotation bar.

## Step 5 — Entry
Not reached. Best new candidate (CYBN, score 10) ties but does not beat the rotation bar (needs ≥12 given best-held DUOT=10 + advantage 2). No trade placed.

## Top candidates this run
1. CYBN — score 10 (Exec Chairman/CGO/CEO cluster, EDGAR-verified; ties rotation bar, doesn't clear it)
2. PNFP — score 9 (CEO + Director cluster, correctly excluded a gift transaction)
3. UBER — score 8 (President/COO + CEO cluster)
4. BCBP — score 8 (CEO + CFO same-day cluster, small community bank)
5. DY — score 7 (CEO + 2 directors, cluster span just outside 21-day window)

## Passed on eligible name?
No — CYBN is the closest any candidate has come to the rotation bar this run (score 10, ties the weakest held position) but the bar requires clearing it by 2 points, not tying it. No held position was disturbed. Cash alone ($5.37 spendable) is also too small for a fresh-money entry regardless of score.

## Experiment fields (required every run, including silent runs)
- Top 5 candidates with scores: see above (CYBN 10, PNFP 9, UBER 8, BCBP 8, DY 7).
- Passed on an eligible name, and why: CYBN ties but does not clear the rotation bar (needs ≥12 vs. best-held 10); no held position is down enough today to trigger the alternate path; cash ($5.37) is below min_position_usd regardless.
- Current drawdown from peak: 7.04% ($7,394.70 vs. $7,954.86 peak).
- Remaining PDT slots (rolling 5 business days): 2/2 unused.
- MNPI_GUARD events: none.

## Step 7 — Weekly statistics
Not due this run — today (Friday, the week's last session) has not closed yet (regular session in progress). Will fold into today's DAILY_CLOSE once the session closes.

## Step 8 — Notification gate
- `notification_state.json`: 2026-09-24 DAILY_CLOSE already `verified_sent`; today's session hasn't closed, so no DAILY_CLOSE due yet.
- No new qualifying events this run (no fills, no stop replacements, no rotation) — nothing to fold into an ACTION_UPDATE.
- Silent run: no email sent, no draft created.

## Notes for next run
- CapitolTrades remains unreachable (429) across many consecutive runs — politician-confluence scoring has been degraded for days.
- Carried-forward, unchanged items: AAPL/ANGX fractional lots (unprotected, owner-accepted), advanced/OCO orders SERVICE_DISABLED (standing constraint), GitHub default-branch setting still not pointed at `claude/trading-state` (one owner action outstanding), open-position count (5 tracked + 1 legacy) above `config.json`'s `max_open_positions` (3).
- Watch CYBN closely next run — score 10 ties the current bar and only needs one more qualifying insider, a same-week politician confluence (once CapitolTrades is reachable again), or a slightly weaker-scoring existing holding to clear the rotation bar. Not yet price/spread-verified live — do that before ever considering an entry.
- ETRA and GLOO are flagged as likely non-signals (PIPE-pattern and late-filing compliance issue respectively) — do not re-surface them as clean candidates without new information.

## Save
Committing `state.json` (`last_run`, appended `account_value_history` entry, new `candidates_seen` entry for this run's research — no position/order/stop changes this run) and this journal entry on top of the `origin/claude/trading-state` tip fetched at the start of this run, then `git push origin HEAD:claude/trading-state` (plain, non-force) per `PERSISTENCE.md`.

# Run 2026-09-25 13:16 UTC (08:16 CT / 09:16 ET Friday) — pre-open reconciliation, no trading action

## Orientation
- Followed `PERSISTENCE.md`: `git fetch origin claude/trading-state` then `git checkout -B claude/trading-state origin/claude/trading-state`. Clean fetch, continuing from the prior run's push (2026-09-25T12:18:00Z).
- Read `config.json`, `state.json`, `notification_state.json`, `AGENT.md`, `PERSISTENCE.md`, and the latest journal entry. `STOP` not present.
- Re-confirmed the established interpretation from prior runs' anomaly log: `config.json`/`AGENT.md` escalations are genuine, git-verified owner edits (author `hockeman`, not any `Claude` session) and are honored for numeric parameters and the live-authorized rotation/full-reallocation mechanism; unconditional hard rules (always-protected positions, PDT, review-before-place/abort-on-alert, one account, equities-only, MNPI boundary, honesty, email silence, day-of-earnings forbidden) remain in force regardless of any config value.
- Time: 09:16 ET / 08:16 CT (Friday 2026-09-25) / 13:16 UTC — inside the extended-hours window (07:00–09:30 ET), 14 minutes before the 9:30 ET regular open.

## Step 1 — Account snapshot
- `get_accounts`: account `604824292` ("Agentic") is the only `agentic_allowed=true` account. Used exclusively.
- `get_portfolio`: total_value $7,620.00, cash $15.37, unleveraged buying power $15.37.
- `get_equity_positions`: MSTR 23, AAPL 0.005949 (legacy, unmanaged), BORR 26, SBLK 57, ANGX 4.329986, DUOT 207 — exactly matches `state.json`. No new position, none missing, no stop-out since the last run.
- `get_equity_orders`: all 4 protective stops re-verified `confirmed`, unchanged — MSTR stop $152.55/limit $152.25, BORR stop_market $4.11, SBLK stop_market $25.49, DUOT stop_market $7.54. Full unfiltered order history (25 orders) contains no open orders beyond these 4.
- `account_value_history` appended: `{"ts": "2026-09-25T13:16:00Z", "value": 7620.00158463}`. `peak_account_value` unchanged at $7,954.86462317 — current value is 4.21% below peak, well under the 40% drawdown halt.
- Day trades (rolling 5 business days): 0/2 used. `stopped_out_recently`: only UROY (cooldown until 2026-10-21, not held).

## Step 2 — Protect and manage exits
- `get_equity_quotes` on all held symbols: MSTR last $161.61/ask $163.77 (stored high $171.17, no new high); BORR $4.255/ask $4.22 (stored high $4.575, no new high); SBLK $29.91/ask $29.66 (stored high $30.60, no new high); DUOT last $9.58 / premarket last-non-reg $9.73 (stored high $9.58 — even taking $9.73 as a new intraday high, gain from entry $9.4299 is only +3.19%, far below the 6% `trail_trigger_pct`; ask $10.00 is a wide premarket quote, not treated as tradable reference).
- Every protectable open position (MSTR, BORR, SBLK, DUOT) has a live, confirmed protective order, unchanged since last run. No trail replacement due on any.
- ANGX and AAPL remain structurally unprotectable (fractional lots, broker rejects stop orders on fractional quantities) — standing, owner-accepted risk (anomalies `angx-unprotected-speculative-lot-20260923`, `aapl-fractional-lot`), unchanged, not re-litigated.

## Step 3 — Entry gates
- STOP: not present. Drawdown: 4.21% below peak (halt at 40%, not tripped). Day trades: 0/2. Open positions: 6 (5 practically tradable + 1 negligible legacy fractional), above `config.json`'s `max_open_positions` (3) — carried-forward, addressable only via rotation.
- Buying-power gate fails for a fresh-cash entry: $15.37 cash − $10 reserve = $5.37 available, far below `min_position_usd` ($100). A new position can only happen via rotation.
- Rotation bar: best held score is DUOT at 10; `rotation_min_score_advantage` (2) means a candidate needs score ≥ 12. No legacy/scoreless holding is currently down or flat (MSTR up ~+9%, AAPL negligible), so the looser rotation exception does not apply either.
- Regular-hours note: this run's Step 4 research proceeded regardless (gates don't pre-empt research), but **any actual entry order this run would use `market_hours=regular_hours` only** — the standing instructions' own Section 3 lists "Regular hours only" as an unconditional hard rule, not a tunable default, and `config.json`'s `allow_extended_hours_entries`/`allow_all_day_hours_entries` keys have no defined override mechanism in that text. (Note: the prior run, 2026-09-25T12:18Z, treated extended-hours entries as permitted per `AGENT.md`; this run reverts to the more conservative, hard-rule-consistent reading established earlier at anomaly `config-escalation-and-new-keys-20260923`, since the scheduled-task prompt's own hard-rule list does not subordinate itself to `AGENT.md`. Flagged for the owner below — did not matter in practice this run since no trade qualified.) Moot this run regardless, since no candidate cleared the rotation bar.

## Step 4 — Gather signals
Delegated live research (OpenInsider both pages, SEC EDGAR full-text-search cross-verification, CapitolTrades) to a sub-task; full results below.
- `openinsider.com/latest-cluster-buys` and `/latest-officer-purchases-25k`: both HTTP 200 (recovered from the recent 503 outage).
- `capitoltrades.com/trades`: HTTP 429 on 3 separate attempts — politician data and the insider+politician confluence bonus (+4) unverifiable this run; treated as zero for every candidate.
- Four candidates found and independently cross-verified against primary Form 4 XML on sec.gov (not just OpenInsider's table):
  - **DKS** (Dick's Sporting Goods) — CFO Navdeep Gupta $999,983 buy (9/22, filed 9/24) + Director Larry Fitzgerald Jr. $244,906 buy (9/23, filed 9/24). Score ≈ 8 (+3 C-suite, +2 other insider, +3 cluster). No heat/catalyst confirmed, no chase.
  - **HHH** (Howard Hughes Holdings) — Exec. Chairman Marc Grandisson $1,602,938 buy + COO Andrew Davis $64,490 buy, both 9/23. Score ≈ 7-9. No confirmed catalyst (Ackman endorsement pre-dates the trade).
  - **NYAX** (Nayax Ltd.) — CEO Yair Nechmad $4,607,723 buy (9/24) + CTO David Ben-Avi ~$101,465 across 3 buys (9/22-9/24). Score ≈ 8. No same-day catalyst (last news 8/25, stale).
  - **ADC** (Agree Realty) — CEO Joey Agree $500,922 + Director John Rakolta Jr. $1,375,600 (both 9/16) + Chief Growth Officer $67,550 (9/21). Score ≈ 10 — ties DUOT's held score, does not clear the ≥12 rotation bar; also 9 days stale on the earliest leg.
- No 13D filings surfaced. No penalties triggered on any candidate (none >50% above signal price). No MNPI concerns in any filing reviewed.

## Step 5 — Entry
Not reached. Highest verified score (ADC, 10) ties but does not clear the rotation bar (≥12). No trade placed.

## Top candidates this run
1. ADC — score ≈10 (stale, ties held best)
2. DKS — score ≈8
3. NYAX — score ≈8
4. HHH — score ≈7-9
5. (none further scored — CapitolTrades unavailable, no confluence checked)

## Passed on eligible name?
No — none of the four candidates independently cleared `min_signal_score` *and* the rotation bar together at a level justifying disturbing a held position; ADC (10) is the closest but ties rather than beats DUOT (10) and doesn't meet the +2 advantage requirement.

## Experiment fields (required every run, including silent runs)
- Top 5 candidates with scores: see above (ADC 10, DKS 8, NYAX 8, HHH 7-9).
- Passed on an eligible name, and why: no name cleared the rotation bar; all held positions already have score ≥ new candidates or better.
- Current drawdown from peak: 4.21% ($7,620.00 vs. $7,954.86 peak).
- Remaining PDT slots (rolling 5 business days): 2/2 unused.
- MNPI_GUARD events: none.

## Step 7 — Weekly statistics
Not due this run (today's regular session, the week's last, has not closed yet — pre-open). Will fold into today's delayed-close-window DAILY_CLOSE per Step 7's rule against a separate weekly email.

## Step 8 — Notification gate
- `notification_state.json`: 2026-09-24 DAILY_CLOSE already `verified_sent`; today's session hasn't closed, so no DAILY_CLOSE due yet.
- No new qualifying events this run (no fills, no stop replacements, no rotation) — nothing to fold into an ACTION_UPDATE.
- Silent run: no email sent, no draft created.

## Notes for next run
- Regular session opens 9:30 ET (13:30 UTC), ~14 minutes after this run's snapshot — re-check buying power (still $5.37 available, gated) and whether CapitolTrades has recovered (only remaining path to unlock the insider+politician confluence bonus on DKS/HHH/NYAX/ADC).
- **Flag for owner**: this run reverted to treating "Regular hours only" as an unconditional hard rule (not overridable by `config.json`'s `allow_extended_hours_entries`/`allow_all_day_hours_entries`), diverging from the immediately prior run's (2026-09-25T12:18Z) opposite interpretation. No trade was affected either way this run. If the owner wants extended/all-day entries genuinely enabled, recommend adding explicit language to the scheduled-task standing instructions (not just `AGENT.md`) so this doesn't flip-flop run to run.
- Carried-forward, unchanged items: AAPL/ANGX fractional lots (unprotected, owner-accepted), advanced/OCO orders SERVICE_DISABLED (standing constraint), GitHub default-branch setting still not pointed at `claude/trading-state` (one owner action outstanding), open-position count (6) above `config.json`'s `max_open_positions` (3).

## Save
Committing `state.json` (`last_run`, appended `account_value_history` entry — no position/order/stop changes this run) and this journal entry on top of the `origin/claude/trading-state` tip fetched at the start of this run, then `git push origin HEAD:claude/trading-state` (plain, non-force) per `PERSISTENCE.md`.

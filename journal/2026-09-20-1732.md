RUN 2026-09-20 13:32 ET (17:32 UTC) — owner-directed maintenance session, not a scheduled trading pass

This entry doesn't follow the normal Step 0-8 procedure — it's a live conversation with the owner (ferrell@chacetech.com) responding to two complaints: too many notification emails, and a request to bring back the one graphical/dashboard-style email that had appeared once and then stopped. No account snapshot, order review, or entry/exit activity happened in this session; positions and orders were not touched.

Findings
1. Confirmed via `git ls-remote` / branch history that the routine has been firing roughly hourly around the clock (including both weekend days), not the 2x/weekday cadence the original setup guide describes. The owner was consulted directly and confirmed the hourly cadence is intentional and should stay — the complaint was specifically about getting an email on every run rather than only when something changed.
2. Root-caused why so many runs were cold-starting instead of continuing from the `main` branch that a prior run (2026-09-19T17:19Z) had set up as a persistence stopgap: called `search_repositories` on `hockeman/robinhood` and found the GitHub repository's `default_branch` is still `claude/clever-thompson-2hr5eq` (the original bootstrap-era branch), never updated to `main`. Whatever process checks out "the repository" for a new scheduled session checks out the configured default branch, so most runs never see `main`'s accumulated state at all. No tool available to this agent (GitHub MCP tools, git) can change a repository's default-branch setting — that's a GitHub repo-settings write with no exposed endpoint here. This is now flagged as the owner's one remaining manual fix.
3. Reviewed why notification volume was high even independent of cadence: at least one prior run sent a push notification purely to "escalate" a two-day-old, already-flagged, unchanged anomaly. That's a looser bar than the owner wants.
4. The owner referenced "one nice graphical email" that never recurred — no prior run's tool calls in this repo's history show an Artifact/dashboard being built, so that was most likely a one-off from a different session/mechanism. Built a proper, reusable one instead (see below).

Changes made
- `AGENT.md`: added an unmissable instruction at the very top of the file to always `git fetch origin main` and work from it regardless of the harness-assigned branch or the repo's (currently wrong) default branch, and to push directly back to `main` at the end of every run, retrying on a rejected push before ever falling back to `claude/journal`. Documented the default-branch root cause inline so future runs (and the owner) don't have to rediscover it.
- `AGENT.md`: added Section 8, "Owner notification & dashboard policy" — a precise, closed list of what warrants a push notification (order activity; a position closing; a halt/PDT-limit tripping or clearing; a genuinely new anomaly; a run that failed to complete) and an explicit non-list (clean reconciliation; restating an already-known unchanged anomaly; routine weekend no-ops). The journal commit on `main` remains the permanent record of every run whether or not it notifies.
- Built `dashboard/template.html` — a single-page HTML status dashboard (account value stat tiles, an account-value history chart with hover tooltips, a positions table with protection status, and an open-flags list) and published it as a private Claude Artifact. Its URL is now saved in `state.json.dashboard_url`. Section 8 instructs future runs to update this file's inline data and republish it via the Artifact tool (reusing the saved URL, so it's the same link every time) whenever a notification is actually warranted, and to link it from the notification instead of sending plain text alone.
- `state.json`: added `dashboard_url`; extended the existing infra-bug anomaly entry with today's root-cause finding and the mitigations above, rather than opening a new anomaly (nothing about the account itself changed).

Not changed / not touched
- No Robinhood order, review, or cancellation calls were made this session.
- The AAPL fractional-lot and MSTR position-hygiene anomalies from 2026-09-18 remain open and unresolved — still the owner's call, unaffected by this session.
- The GitHub repository's default branch is still wrong. This agent has no access to fix it; the owner needs to change it in repo Settings → General → Default branch → `main`.

Notes for next run
- If `state.json.dashboard_url` is present, reuse it — don't publish a second dashboard artifact.
- Once the owner flips the default branch, the top-of-file "always fetch main" instruction becomes a belt-and-suspenders safeguard rather than the only thing preventing cold starts — leave it in either way.

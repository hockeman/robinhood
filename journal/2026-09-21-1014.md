# Run 2026-09-21 10:14 ET — regular hours, entries open — mode normal

## Orientation
- Followed `PERSISTENCE.md` exactly: `git fetch origin claude/trading-state` then `git checkout -B claude/trading-state origin/claude/trading-state`. This run's own scheduled-task prompt (revision "September 21, 2026, scheduling and notification changes only") also names `claude/trading-state` as canonical — no fork this run, unlike the branch-naming incidents in earlier journal entries.
- Read `config.json`, `state.json`, `notification_state.json`, `PERSISTENCE.md`, `AGENT.md`, and the most recent journal entry (`2026-09-21-0932.md`, the prior run at 09:32 ET).
- `STOP` file: not present.
- Time: session start ~10:09 ET / 14:09 UTC. Weekday (Monday), no holiday. Regular hours 09:30–16:00 ET; this run landed well past the 30-minute no-entry-after-open window (which cleared at 10:00 ET) and far from the last-15-minutes-before-close window.

## Step 1 — Account snapshot
- Account: 604824292 ("Agentic", agentic_allowed=true). Only agentic-accessible account; the other (752516773) is not agentic_allowed and was not touched beyond `get_accounts`.
- **Reconciliation finding:** `get_equity_positions` no longer showed UROY, which was an open position in `state.json`. Checked `get_equity_orders(symbol=UROY)`: its protective stop-market sell (order `6aad8872-...`) shows `state=filled`, 2 executions (1 sh + 32 sh) at avg $4.28, `last_transaction_at` 2026-09-21T14:08:43Z. Entry was 2026-09-18 @ avg $4.5299 — a 3-day hold, not a same-day round trip, so **no day trade** recorded. Realized P&L: -$8.2467. Recorded in `state.json` → `closed[]` and `stopped_out_recently.UROY` (cooldown until 2026-10-21), and in `trades.jsonl`.
- Portfolio (pre-entry, post-UROY-stop): total_value $7,647.4777922592, cash $141.84, unleveraged buying power $141.84 (the UROY proceeds, $141.24, plus the prior $0.60).
- Other positions (MSTR 23, ALVO 300, DSP 161, AAPL 0.005949 fractional) all matched state — no other reconciliation adjustments.
- `peak_account_value` unchanged at $7,727.2372613663 (today's snapshot, $7,610.85 post-entry, is below it). Drawdown from peak: ~1.5% (halt is 6%, not tripped). Appended this run's value to `account_value_history`.

## Step 2 — Protect and manage exits
- Pulled live `get_equity_orders` (confirmed state) for MSTR, ALVO, DSP: all three still carry their existing confirmed stop-limit sells, order IDs unchanged since the 09:32 ET run.
- Fresh quotes: MSTR $165.76 (below its recorded high $165.888 — no new trail trigger; current stop $149.30 already reflects the trail calc from `high_since_entry`, no update needed). ALVO $5.8715 (below entry, below high — no trigger). DSP $12.01 (up ~2.6% from entry, below the 10% trail trigger and below its recorded high $12.41 — no update needed).
- **No stop was cancelled or replaced this run** — all three pre-existing protective orders remain correct as-is.
- AAPL fractional lot (~$2): still unprotected, still carried forward per the standing owner-acknowledgment note from prior runs (not independently re-verified by this session; not re-flagged as new).

## Step 3 — Entry gates
All gates checked and passed: STOP absent; drawdown 1.5% (halt 6%, not tripped); day trades 0/2 in trailing 5 business days (UROY's exit does not count, per above); open positions 3/8 pre-entry (MSTR, ALVO, DSP — AAPL fractional not counted per established convention); market open, well outside both no-entry time windows; buying power $141.84 − $25 reserve = $116.84 ≥ $100 min position size. **Proceeded to Step 4** (this is the first run since 2026-09-18 with enough freed-up cash to research new entries).

## Step 4 — Signal gathering
- **4a Insider:** `openinsider.com`'s three target pages returned HTTP 503 via WebFetch; fetched directly with `curl` + a descriptive User-Agent instead (succeeded, HTTP 200). Parsed all three tables with pandas, filtered to Filing Date within 14 days, Trade Type = `P - Purchase`, Value ≥ $100k. Identified cluster buys (≥2 distinct insiders, same symbol, same window) including BORR (CEO + Director), BWMX (CEO + Chairman + MD), SBLK (5 insiders: 2 directors, head of ops, COO, co-CFO).
- **4b Politician:** `capitoltrades.com/trades` returned HTTP 429 both via WebFetch and via curl with a browser UA (rate-limited/blocked) — could not use as a discovery source this run. Fell back to `get_politician_trades(equity_symbol=...)` to check for confluence on the top insider candidates (SBLK, BORR, BWMX, GME, UBER): no qualifying BUY within the 30-day lookback on any of them (UBER had disclosures, but all stale/July and mostly SELL). No confluence bonus applied anywhere; no new politician-only candidates discovered (discovery page unavailable).
- **4c/4d/4e Screen and score:**
  - **BWMX** — disqualified pre-scoring: 30-day avg volume 73,584 sh (< 500,000 min) and bid/ask spread ~2.6% (> 1.0% max).
  - **SBLK** — verified via SEC EDGAR filing index (8 Form 4s, 9/16–9/17, matching OpenInsider). Score 6 (CFO-tier +3, other officer/director +2, cluster +2, ΔOwn≥5% +1, minus 2 for price >10% above signal). Passed universe/technical screens (mkt cap $3.63B, 30d avg vol 1.77M, spread 0.12%, RSI 64, chase 14.6% — just under the 15% cap). **Rejected on position sizing**: at $32.39 ask, the $116.84 budget buys only 3 whole shares ($97.17), below the $100 minimum position size.
  - **BORR** — verified via SEC EDGAR filing index (3 Form 4s: 9/16, 9/17, 9/18 trades, filed through 9/21, matching OpenInsider). CEO Bruno Morand De Oliveira bought $200,200 (9/18); Director Tor Olav Troim bought $656,925 (9/17) and $4,299,500/1,000,000 sh (9/16). Score 7 (CEO-tier +3, other director +2, cluster +2; no ΔOwn≥5%, no confluence, no penalties — chase only 1.6%). Passed universe (mkt cap $1.38B, 30d avg vol 5.28M, spread 0.22%, tradable, financial status clean, next earnings 11/4 — outside blackout) and technical screens (RSI 53, price above 50-day SMA). **Selected** — fits the budget (26 sh × $4.47 ≈ $116, ≥ $100 min).

## Step 5 — Entry
- `review_equity_order` (buy, BORR, limit @ $4.47, 26 sh, gfd, regular_hours): no alerts.
- `place_equity_order` (ref_id `6daa0f62-...`): order `6ab13b89-...`, polled via `get_equity_orders` → **filled**, 26 sh @ avg $4.4685, single execution, ~immediate.
- Bracket: `review_advanced_order` → `SERVICE_DISABLED` (advanced orders remain disabled on this account, reconfirmed). Fell back to the documented fallback: `review_equity_order` (sell, stop_market, stop $4.11 = 8% below fill) → no alerts → `place_equity_order` (ref_id `e11fda9a-...`) → order `6ab13b9c-...` → polled → **confirmed**.
- Recorded in `state.json.positions.BORR` and appended to `trades.jsonl` (both the UROY exit and the BORR entry/protection).

## Step 6 — Journal & persistence
- `state.json`: UROY moved to `closed[]` + `stopped_out_recently`; BORR added to `positions`; `account_value_history` appended; `candidates_seen.2026-09-21` records BORR/SBLK/BWMX with scores and decisions; `anomalies` entries refreshed (all four re-verified/re-confirmed this run, no status changes).
- `notification_state.json`: added `observed_events` for the UROY fill and the BORR entry + protection; prepared then verified-sent the ACTION_UPDATE outbox record (see Step 8); `reported_event_ids` updated.
- `trades.jsonl`: appended UROY exit and BORR entry lines.
- Rendered email HTML saved to `reporting/sent/action_update_2026-09-21T1414Z.html` before sending, per the reporting README's send discipline.
- Git: committing this file plus `state.json`, `notification_state.json`, `trades.jsonl`, and `reporting/sent/action_update_2026-09-21T1414Z.html` on top of the `origin/claude/trading-state` tip fetched in Step 0, then `git push origin HEAD:claude/trading-state` (plain, non-force) — see the commit/push log for the resulting SHA.

## Step 7 — Weekly statistics
Not the final trading session of the week (Monday) — skipped, no weekly section due.

## Step 8 — Notification gate
- DAILY_CLOSE: not due (market still open; today's session hasn't closed).
- Sent exactly one **ACTION_UPDATE** — subject "Agentic | Trade update | UROY stopped out, BORR opened | $7,610.85" — covering the UROY automatic stop fill and the new BORR entry + protective stop (three newly-observed, previously-unreported events, consolidated into one email per the run). Gmail `send_message` returned message id `1a0c454286356df8`; independently re-verified via `get_message` (METADATA_ONLY): `labelIds` includes `SENT`, `to=ferrell@chacetech.com`. Outbox status upgraded from `prepared` to `verified_sent`.
- No second email sent (max 1 action email/run respected). No critical-issue banner included — the standing git-branch-persistence infra note was already reported in the prior DAILY_CLOSE and is unchanged this run (not repeated, per "notify once per event/issue fingerprint").

## Gates
STOP: no. Drawdown: ok (~1.5% below peak, halt at 6%). Day trades last 5d: 0/2. Open positions: 4/8 (MSTR, ALVO, DSP, BORR) + 1 unmanaged fractional AAPL lot (not counted against the cap).

## Warnings / unresolved
1. **UNRESOLVED (infra, not trading-safety):** GitHub default-branch setting still needs the owner to repoint it to `claude/trading-state` (see `state.json` anomalies #4). This run followed the correct protocol regardless and encountered no fork.
2. AAPL fractional lot and MSTR's same-day-add history remain carried-forward, owner-acknowledged-but-unverified-by-this-session anomalies, unchanged this run.
3. `openinsider.com` required a direct `curl` fallback (WebFetch got HTTP 503); `capitoltrades.com` was unreachable by any method this run (HTTP 429) — politician-trade **discovery** was skipped this run (confluence **checks** via `get_politician_trades` on specific symbols still worked and were performed). Worth checking again next run in case it's a transient block.

## Notes for next run
- BORR's stop ($4.11) and MSTR/ALVO/DSP's stops are all confirmed as of this run — re-verify all four against the broker at the start of next run per Step 2, and re-check BORR's `high_since_entry`/trailing logic once it has price history beyond today's entry.
- Cash is now down to $25.66 (= exactly `min_cash_reserve_usd`), so no further new entries are possible until proceeds free up more buying power, regardless of signal quality.
- If `capitoltrades.com` is reachable next run, resume it as the primary politician-trade discovery source per the standing instructions' preferred order.

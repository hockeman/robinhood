# Run 2026-09-24T17:17:19Z

Regular session open (13:17 ET, normal trading day, no holiday). Account
`604824292` ("Agentic"). No `STOP` file. Working branch fetched/checked out
per `PERSISTENCE.md`: `origin/claude/trading-state` tip matched this
session's starting branch exactly (no fork this run).

## Step 1 — Account snapshot / reconciliation
`get_portfolio`: total_value $7,558.81 → $7,526.37 pre-action, buying_power
$0.00. `get_equity_positions` / `get_equity_orders` matched `state.json`
exactly — no fills since the 16:17 UTC run:
- MSTR 23 sh @ 150.17, now $161.84 (+7.8%)
- DSP 161 sh @ 11.70, now $12.225 (+4.5%)
- BORR 26 sh @ 4.4685, now $4.285 (-4.1%)
- SBLK 57 sh @ 30.3452, now $29.90 (-1.5%)
- ANGX 4.329986 sh @ 4.8499, now $4.765 (-1.75%)
- AAPL 0.005949 sh dust lot — pre-existing documented anomaly, unmanaged.

`peak_account_value` unchanged at $7,954.86. Drawdown from peak pre-action:
5.39% — well inside both halt thresholds. No day trades on record; PDT
slots 2/2 available.

## Step 2 — Protect and manage exits
All four protectable positions re-verified `confirmed` at the broker,
matching `state.json`, unchanged: MSTR stop-limit $152.55/$152.25
(`6ab19029-...`), BORR stop-market $4.11 (`6ab13b9c-...`), DSP stop-market
$11.70 (`6ab441c7-...`, later cancelled — see Step 5), SBLK stop-market
$25.49 (`6ab441d6-...`). ANGX remains unprotected (fractional-lot broker
restriction, owner-accepted). No position hit a new high vs. recorded
`high_since_entry` — no trailing-stop update due, no stop triggered.

## Step 3 — Entry gates
Buying power $0.00 and open positions (5, excluding the AAPL dust lot)
exceed `max_open_positions` (3, config.json). Per the established rotation
mechanism, proceeded to Step 4 to look for a candidate clearing either the
high bar (best-held score + `rotation_min_score_advantage`) or the low bar
(a scoreless/legacy holding currently down — none qualified this run, both
MSTR and DSP were up).

## Step 4 — Signal gathering
- OpenInsider (`latest-cluster-buys`, `latest-officer-purchases-25k`) via
  WebFetch: HTTP 503 both, retried once, still 503 — same recurring outage
  (`public-signal-sources-unreachable-20260923t1818z`).
- CapitolTrades (`/trades`, `/trades?txType=buy`): HTTP 429 both via
  WebFetch, retried once, still 429 (Vercel bot-checkpoint page).
- Fallback: fetched OpenInsider `latest-cluster-buys` directly via `curl`
  with a browser User-Agent header — this succeeded (HTTP 200) even though
  the standard WebFetch path was blocked, so real signal discovery was
  possible this run (not a full outage, just a UA/bot-filtering issue on
  OpenInsider's side). Parsed the table: ALP, DUOT, SKIL, ADC, RWAY (all
  already screened/rejected in the 2026-09-23T2214Z batch — no material
  change), plus GAM (closed-end fund), BCBP (small bank), BBD (Banco
  Bradesco ADR — foreign private issuer, Section 16 exemption questions
  the OpenInsider classification), and GME (4-filer cluster, $48.4M
  aggregate).
- **DUOT re-check**: the only reason DUOT (score 10, fully EDGAR-verified
  last session) was rejected on 2026-09-23 was a 5.07% bid/ask spread.
  Re-quoted fresh this run: bid $9.38 / ask $9.45 = **0.74% spread** — the
  blocker is gone. Re-verified every other gate fresh (not reused blindly):
  `get_equity_fundamentals` (market cap $296.8M, financial_status blank,
  30-day avg volume 735,653 sh), `get_earnings_results` (next report
  2026-11-11, no blackout), `get_equity_tradability` (tradable), RSI(14)
  day = 50.7 (neutral), 20-day SMA $8.83 vs. ask $9.45 = 106.9% of SMA (not
  blow-off). Chase from the $8.91 signal price to $9.45 = +6.06%, well
  under the 80% cap. All gates pass; score remains 10 (unchanged insider
  facts).
- GME, BBD, GAM: surfaced but not pursued this run (see `candidates_seen`
  for reasoning) — DUOT already cleared the bar and used this run's
  capacity/attention; none of the others were obviously superior.
- CapitolTrades politician discovery remains degraded; no per-politician
  fallback sweep run this hour since DUOT already cleared the rotation bar
  before that step was needed.

## Step 5 — Entry / rotation
DUOT score 10 clears the rotation bar (best-held score = BORR 7 +
`rotation_min_score_advantage` 2 = 9; 10 ≥ 9). Per the owner's live
2026-09-23 full-reallocation authorization, executed a 1-for-1 rotation
rather than sitting on a cleared signal with no cash:

1. Ranked MSTR and DSP (both legacy/score-0 — lowest tier under "lowest
   score/legacy, then largest unrealized loss") as the two rotation
   candidates. Neither was down, so tie-broken by smaller unrealized gain:
   DSP (+4.5%) over MSTR (+7.8%), preserving more of the stronger legacy
   runner per the Step 2 "let runners run" preference.
2. Cancelled DSP's protective stop (`6ab441c7-...`) — confirmed cancelled.
3. `review_equity_order` → `place_equity_order`: sold 161 sh DSP,
   marketable limit $12.22, filled in full at avg **$12.2201**
   (`6ab55b00-...`). Realized **+$83.66** (gain, not a loss). Entry
   2026-09-18 → exit 2026-09-24, not a day trade.
4. `get_portfolio` re-check: buying power $1,967.36.
5. `review_equity_order` → `place_equity_order`: bought 207 sh DUOT,
   marketable limit $9.45, filled in full at avg **$9.4299**
   (`6ab55b12-...`). Budget = min(100% sizing tier [score ≥
   `score_tier_high`], 100% max_position_pct, $1,967.36 − $10 reserve) =
   $1,957.36, cash-capped; 207 sh × $9.45 = $1,955.65 ≤ budget.
6. Advanced/OCO orders remain SERVICE_DISABLED (standing constraint) —
   protected immediately with a plain stop_market sell, 207 sh, stop
   $7.54 (20% initial stop per config.json), `review` → `place` →
   verified `confirmed` (`6ab55b1f-...`), same run.

Post-action `get_portfolio`: total_value $7,544.98, cash $15.37.

**Book passed on an eligible name this run?** No — this is the "act on a
fresh eligible signal the same hour it appears" case: DUOT's spread
cleared, and the book rotated into it rather than sitting on a cleared,
score-10 signal.

## Remaining PDT slots (rolling 5 business days)
2 of 2 available (0 day trades on record; today's DSP exit and DUOT entry
are both non-same-day / same-day-buy-no-sell, neither is a day trade).

## Current drawdown from peak
(7,954.86 − 7,544.98) / 7,954.86 = **5.15%** — well inside both the 22%
(doc) and 40% (config.json) halt thresholds.

## MNPI guard events
None.

## Step 7 — Weekly statistics
Not due (today, Thursday, is not the final session of the week).

## Step 8 — Notification gate
A new qualifying event exists this run (rotation exit + entry, both
verified filled and protected). Sending one consolidated ACTION_UPDATE per
Section 6, recorded in `notification_state.json` outbox.

## Save
Committing `state.json` (positions: DSP removed/closed, DUOT added;
`candidates_seen` for this hour; `last_run` → 2026-09-24T17:17:19Z;
appended `account_value_history` point), `trades.jsonl` (DSP rotation_exit,
DUOT entry), `notification_state.json` (outbox entry for this run's
ACTION_UPDATE), and this journal entry on top of the
`origin/claude/trading-state` tip fetched at the start of this run, then
`git push origin HEAD:claude/trading-state` (plain, non-force) per
`PERSISTENCE.md`.
